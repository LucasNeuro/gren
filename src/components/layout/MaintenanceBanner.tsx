'use client';

import React from 'react';
import { Alert } from '../ui/Alert';

// Componente simplificado para evitar erros no deploy
// Removido a dependência de Edge Config (que pode não estar configurado)
export const MaintenanceBanner: React.FC = () => {
  // Por enquanto, não exibe nada (pode ser reativado quando o Edge Config estiver configurado)
  return null;
};
