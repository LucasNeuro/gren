{
  "version": 2,
  "builds": [
    {
      "src": "grenflow/package.json",
      "use": "@vercel/next",
      "config": { "installCommand": "cd grenflow && npm install", "buildCommand": "cd grenflow && npm run build" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "grenflow/$1" }
  ]
}
